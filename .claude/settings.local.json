{
  "permissions": {
    "allow": [
      "Bash(powershell -Command \"Add-Type -AssemblyName System.Speech; $speak = New-Object System.Speech.Synthesis.SpeechSynthesizer; $speak.Speak\\(''Claude voice system is now working''\\)\")"
    ]
  }
}
