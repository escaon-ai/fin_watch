# Required packages for the financial analysis agent

# Install these packages to your renv environment:
# install.packages(c("emayili", "ellmer"))

# If ellmer is not available on CRAN, you might need to install it from GitHub:
# remotes::install_github("rpkgs/ellmer")

# For Gemini API access, you might alternatively use:
# install.packages("googleCloudRunner")  # For Google Cloud services
# Or use the REST API directly with httr package